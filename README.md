# Python-based-Audiobook-Creator
This project is a Python-based Audiobook Creator. It allows a user to select a PDF file from their computer, extracts the text content page-by-page, and converts that text into audible speech in real-time.
